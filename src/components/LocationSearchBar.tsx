import React from "react";
/**
 * LocationSearchBar
 *
 * A search input powered by Mapbox Geocoding.
 * Allows users to search for a location and select its coordinates.
 *
 * @param props - Props containing the onSelect callback
 * @returns JSX.Element
 */
const LocationSearchBar: React.FC = () => {
  return <div id="geocoder" className="w-full" />;
};

export default LocationSearchBar;
